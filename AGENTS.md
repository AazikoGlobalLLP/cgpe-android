# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

<!-- BEGIN CODEX WORKFLOW BRIDGE -->
## Codex project workflow

- Communicate in natural Roman-script Hinglish; keep identifiers, paths, commands, and code unchanged. Write durable technical documentation in English.
- Read `docs/CODEX_WORKFLOW.md`, then this project's `CLAUDE.md` and its applicable references. Resolve instruction scope and command sources as described in the workflow. Do not recursively reload the `CLAUDE.md` → `AGENTS.md` reference.
- Treat `/boot`, `/bootstrap`, `/handoff` and their `Run the ... workflow` equivalents as requests to execute the discovered Markdown workflows, never as shell commands. Read each complete definition before executing it.
- Start with boot orientation: handoff → current phase board → project map → `../contracts/INBOX.md` → relevant spec. Verify open `cgpe-mobile` items against current source. Standalone boot ends with the five-line summary and waits for `go`.
- `go` executes the last identified scope; `continue` resumes authorized unfinished work. Keep going through explicitly authorized multiple phases, with specs and checkpoints; generic one-phase/context rules do not cancel that authorization.
- This repo owns `cgpe-mobile`; shared contracts live only at `../contracts/`. Preserve concurrent work and check current contents before writes. Never tick another session's or the owner's checkbox.
- Follow the verified `Shivam` / `aaziko` policy in the workflow for authorized implementation. Boot and workflow setup do not authorize implementation, commit, push, merge, deployment, or external messages. Preserve `.claude/settings.json` and existing user files.
- Refresh `docs/STATUS.md` at the end of each working session. Distinguish implemented, tested, committed, pushed, merged, deployed, and device-verified states; historical memory is not current proof.

Maintain this single managed section in place; preserve all instructions outside its markers.
<!-- END CODEX WORKFLOW BRIDGE -->
