{
  "//": "Self-contained tsconfig for the Playwright harness. The app's root tsconfig EXCLUDES this folder so `npx tsc --noEmit` (a green gate) never sees these specs; this file exists only so the harness type-checks on its own and editors resolve @playwright/test + node globals.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "CommonJS",
    "moduleResolution": "node",
    "lib": ["ES2022", "DOM"],
    "types": ["node"],
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "noEmit": true
  },
  "include": ["**/*.ts"]
}
