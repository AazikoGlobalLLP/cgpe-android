# HANDOFF — CGPE Connect (Android) — Phase 80 — 2026-08-27

## Done

The owner's instruction was **"no APK now — finish everything first."** So this phase is defined as
everything that could be finished without the owner, a device, or a merge. The biggest item was not
on any list.

- **117 places in the app were showing English that had already been translated.** A scan compared
  every hardcoded literal in `src/` against the **values** of the English dictionary. The copy
  existed in gu/hi/hi-en/gu-en — supplied weeks earlier — and **no screen ever read it.** **73 sites
  are now wired across 42 keys, at zero copy cost.**
- **The most valuable seven are the daily clock-in flow.** Clock-in, clock-out and both break paths
  answered a failed punch in English no matter which language the advisor had chosen. That is the
  screen every field advisor touches every day.
- **Batch 6 is extracted, so the extraction backlog is now clear** — 111 strings quoted verbatim plus
  ~70 more as whole tables, generated by script from source rather than transcribed.
- **`docs/OWNER-ACTIONS-2026-08-27.md` is new**: one page, plain language, everything waiting on the
  owner, with its two server claims re-verified live rather than copied forward.
- **The owner answered the bug-#8 question**: the bottom tab bar **is** still visible while the
  screen is blank.

Gates: `tsc` **0** · `npm test` **1069** (unchanged — this phase adds no logic) · `eslint` **0 errors
/ 12 warnings** (baseline, verified cache-free with `npx eslint src`).
Device-unverified. **No APK is possible until 1 Sep 2026.**

## Files changed

- `src/app/(tabs)/home.tsx` — the seven clock/break failure notices now read `common.offlineBody`;
  two `Open claims` labels; **two `useCallback` dep arrays gained `t`**.
- `src/app/(tabs)/{claims,clients,leads,more,search}.tsx` — filter chips, status stats and page
  headers.
- `src/app/{agent-map,analytics,attendance,calendar,campaigns,claim-new,contests,profile,segments,task-edit,task-new,whatsapp/index}.tsx`,
  `src/app/{client,lead,team}/[id].tsx`, `src/screens/dashboards.tsx` — stat tiles, duty pills, the
  task Due/Priority controls (Priority is now complete, 3 of 3).
- Four files gained `import { useT }`; six components gained `const t = useT()`.
- `docs/i18n/COPY-REQUEST-2026-08-26.md` — **Batch 6 (6a/6b/6c)** written out; three stale claims in
  it corrected.
- `docs/OWNER-ACTIONS-2026-08-27.md` *(new)*, `docs/STATUS.md` *(rewritten)*, `docs/PHASES.md`,
  `docs/DECISIONS.md`.

Commits `9074d08` · `d746d39` · `654ab83`, pushed to `aaziko/Shivam`.

## Decisions made

*(Full text in `docs/DECISIONS.md` under 2026-08-27 (later). The four that will bite someone:)*

- **The seven clock-flow notices do NOT contradict Phase 78's `common.offlineBody` refusal.** Phase
  78's "zero sites match it verbatim" is still true **of the 39 empty-state variants**, which each
  name *what* failed and must not be collapsed. These seven are **write-failure** notices — a
  different set of strings. **Both statements are true; the obvious "correction" in either direction
  is wrong**, and all three places that record it now say so.
- **Six categories were deliberately not swapped**, each for a different reason: a value persisted to
  AsyncStorage, strings that are backend **data**, module-scope label tables (a half-wired menu is
  worse than an English one), module-scope date formatters, the four strings with **no non-React
  translator**, and the file where `t` is a local variable.
- **A partial sweep is only acceptable if its closing copy ships in the same session.** It leaves
  groups visibly half-translated. That is Batch 6a, and it is now the top copy ask.
- **The first commit message said 61 sites; the diff says 73.** Amended before pushing. **Count from
  the diff, not from arithmetic in your head.**

## Known broken / deliberately skipped

- 🔴 **Backend Phase 94 is still not deployed** — re-verified live today: prod `origin/main` is
  `990c660` and `fda199c` is not an ancestor. Video upload and the claim↔file link still fail on a
  phone. The app is correct for both backends.
- 🔴 **Storage is still off** — `cloudStorageConfigured:false`, re-probed today.
- 🔴 **No APK until 1 Sep 2026** — free-plan quota; nothing from Phases 77–80 is on a phone.
- ⚠️ **Several groups are now visibly half-translated** — the accepted cost of the sweep. **Batch 6a
  (70 strings) is exactly the copy that closes them**, and the owner has said they will start it
  later.
- ❌ **Bug #8 is still open.** The owner confirmed **the tab bar stays visible**, which excludes a
  root unmount / whole-app crash and independently confirms the new `ErrorBoundary` is not the fix.
  It does **not** prove native screen detach — detach is merely the last hypothesis standing. **The
  discriminator that actually splits paint-from-render is still unmade:** a `uiautomator dump` while
  blank (text nodes present ⇒ paint problem; absent ⇒ render/data problem). **Do not ship
  `detachInactiveScreens={false}` on the strength of one answer.** The owner has deferred this.
- **No `contracts/` change was needed** — this phase is app-only, and the OPS asks were already
  filed. `INBOX.md` was not written to. No sibling session needs notifying.

## Next session starts here

- **Phase 81: wire Batch 6a the moment the copy arrives** (70 strings, closes the half-translated
  groups), then Batch 5 (49, sign-in), 6b (41, outage), 6c (the menus). **The APK is Phase 82, on or
  after 1 Sep 2026.** Both are owner-gated; if neither has moved, there is no self-contained app-side
  work left that does not first need copy or a device.
- **First command: `/boot`**
- **Watch out for:** ⚠️ **do not "correct" either half of the `common.offlineBody` record** — the
  39 empty-state sentences and the 7 write-failure notices are different string sets and both notes
  are right. ⚠️ **adding `t()` inside a `useMemo`/`useCallback` needs `t` in the dep array**, and
  `tsc` + 1069 tests stay green without it — only cache-free `npx eslint <file>` catches it.
  ⚠️ **re-run the free-wins scan after every copy drop**; a supplied key with no consumer is
  invisible to the parity test, exactly like Phase 79's zero-consumer `channel` field.
